# Results

This folder contains generated CSV outputs from the experiment pipeline.

Run the experiment scripts in `../experiments/` to regenerate these files. Figures are saved separately in `../figures/`.
